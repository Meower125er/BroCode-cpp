#include <iostream>
#include <cmath>

using namespace std;

int main() {
    double x = 3;
    double y = 4;
    double z;

    z = max(x, y);
    // z = min(x, y);
    // z = pow(2, 3);
    // z = sqrt(9);
    // z = abs(-3); negative --> positive
    // z = round(3.14);
    cout<<z;
}